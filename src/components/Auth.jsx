import React from 'react'
import { useState } from 'react'

export const Auth = (props) => {
    console.log(props)
    
      return (
        <div> 
          {props.auth}
        </div>
      );
}
